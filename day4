//functions
all functions in dart will return values
void main(){
int functionanme(){
print("hello");
}
}
-// this another method
void main(){
  peremeter();
  
}
void peremeter(){
  int length= 4;
  int breadth=2;
  int abc =2*(length+breadth);
  print("the premi is $abc");
}

// function where we can declare in parameters inside

void main(){
  peremeter(3,4);
  
}
void peremeter(length,breadth){
  int abc =2*(length+breadth);
  print("the premi is $abc");
}

// with return type and without return type( if we didnot return ,we will get null inplace of abc(2,3)
void main(){
  perimeter(2,3);
int cv=  abc(3,4);
  print("the hsgds is $cv");
  
}
void perimeter(int length, int breadth){
  int sdf=2*(length+breadth);
    print("the area is $sdf");
  
}
int abc(int length, int breadth){
  int sdf=2*(length+breadth);
   return(sdf);
  }
  
  
  
  
  
